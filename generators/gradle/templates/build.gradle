apply plugin: 'scala'

repositories {
    jcenter()
}

dependencies {
    compile 'org.scala-lang:scala-library:<%= scalaVersion %>'
}

apply plugin:'application'
mainClassName = 'be.icteam.demo.Program'